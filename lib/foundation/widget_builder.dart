import '../src/widget/build/builder_function.dart';

WidgetBuilder widgetBuilder(WidgetBuilder builder) => builder;

NullableWidgetBuilder nullableWidgetBuilder(NullableWidgetBuilder builder) =>
    builder;

IndexedWidgetBuilder indexedWidgetBuilder(IndexedWidgetBuilder builder) =>
    builder;

NullableIndexedWidgetBuilder nullableIndexedWidgetBuilder(
  NullableIndexedWidgetBuilder builder,
) =>
    builder;

NonnullWidgetMapBuilder nonnullWidgetMapBuilder(
        NonnullWidgetMapBuilder builder) =>
    builder;

WidgetMapBuilder widgetMapBuilder(WidgetMapBuilder builder) => builder;

NullableWidgetMapBuilder nullableWidgetMapBuilder(
        NullableWidgetMapBuilder builder) =>
    builder;

NonnullTransitionBuilder nonnullTransitionBuilder(
        NonnullTransitionBuilder builder) =>
    builder;

TransitionBuilder transitionBuilder(TransitionBuilder builder) => builder;

NullableTransitionBuilder nullableTransitionBuilder(
        NullableTransitionBuilder builder) =>
    builder;
