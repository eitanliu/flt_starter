import 'package:flutter/widgets.dart';

class ConstBoxWidget extends StatelessWidget {
  static const widget = ConstBoxWidget();

  const ConstBoxWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
